# Crowdin pilot

This integration remains reversible and is being validated on the
`codex/crowdin-pilot` branch. It does not change the application runtime format.

## What remains authoritative

- English messages, stable IDs, and descriptions remain in TypeScript/TSX.
- `src/locales/registry.ts` remains the locale and alias registry.
- `src/locales/lang/*.json` remains the reviewed application catalog format.
- `whitelist_<locale>.json` continues to identify values intentionally kept in
  English.
- `src/locales/lang.compiled` remains a generated production artifact.

Crowdin is only the hosted editing and review layer. Files under
`src/locales/crowdin` are ignored exchange artifacts generated by FormatJS.

## Locale coverage

Crowdin manages every repository locale available in Crowdin.com's standard
language catalog: 45 target languages. Explicit mappings in `crowdin.yml`
preserve repository names for regional locales such as `ca-ES`, `en-AU`,
`pt-BR`, `pt`, `zh-CN`, and `zh-TW`.

Crowdin.com does not currently list Toki Pona (`tok`) or North Korean Korean
(`ko-KP`). Those two catalogs remain managed by the existing repository tools;
they must not be mapped to a different language merely to place them in
Crowdin.

## Local commands

Generate the source catalog with all translator descriptions:

`pnpm run translations crowdin extract`

Generate filtered seed catalogs. Values that merely repeat English are omitted,
except for keys explicitly listed in that locale's whitelist:

`pnpm run translations crowdin seed`

Pass explicit locale codes to seed only a selected subset.

After downloading translations with Crowdin CLI, validate and merge them into
the application catalogs:

`pnpm run translations crowdin import`

The import is deliberately an overlay during the pilot: missing downloaded
values do not erase an existing translation, unknown IDs fail, whitelisted
values stay English, and FormatJS verifies ICU structure before writing.

## Crowdin setup and approval baseline

1. Apply for and receive the free Crowdin Open Source license.
2. Create a public project with English as the source language.
3. Add all target languages represented in `crowdin.yml`.
4. Create a personal token with project read access plus source-string and
   translation read/write access.
5. For local testing, export `CROWDIN_PROJECT_ID` and
   `CROWDIN_PERSONAL_TOKEN`; never add either value to `crowdin.yml`.
6. Generate the source and seeds, then use Crowdin CLI to upload them once:
   `pnpm dlx @crowdin/cli upload sources` followed by
   `pnpm dlx @crowdin/cli upload translations --auto-approve-imported
--import-eq-suggestions`. This grandfathers repository translations as the
   approved baseline.
7. Download with `pnpm dlx @crowdin/cli download translations
--skip-untranslated-strings --export-only-approved`, then run the import command and inspect the
   resulting locale diff.

New community translations remain unapproved until a Crowdin proofreader
approves them. The recurring workflow never uploads target catalogs, so later
repository runs cannot accidentally auto-approve contributor changes.

## GitHub Actions synchronization

After adding `CROWDIN_PROJECT_ID` and `CROWDIN_PERSONAL_TOKEN` as repository
Actions secrets, the `Crowdin Translations` workflow uploads current English
source messages, downloads approved translations, validates every downloaded
locale, and opens or updates one rolling translation pull request.

The workflow is scheduled daily at 09:17 UTC and can also be dispatched
manually. Pushes to `codex/crowdin-pilot` trigger it while the integration is
under test.

GitHub runs scheduled and manually dispatched workflows only after the workflow
exists on the default branch. The daily schedule therefore becomes active only
after the integration itself is accepted into the default branch.

## Acceptance checks

- Crowdin displays source descriptions and ICU variables correctly.
- English placeholders are not counted as translated.
- Intentionally English whitelist entries survive the round trip.
- Downloaded updates change only locale catalogs with newly approved content.
- The pull request passes the complete test and build pipeline.
- Removing the pilot requires deleting only the Crowdin project/configuration,
  the ignored exchange directory, the workflow, and the Crowdin command module.
